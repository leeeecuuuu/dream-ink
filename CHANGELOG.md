# 更新日志

## 2026-05-10

### 新增

- 新增生成失败诊断面板，可根据错误信息识别 API 配置缺失、请求超时、网络/代理异常、模型名错误、尺寸不支持、参考图过大等常见问题。
- 诊断面板提供快捷操作：打开 API 配置、降低质量重试、清空垫图重试、直接重试、复制调试日志。
- 重做任务队列面板，支持展示等待、生成中、成功、失败、取消、取消中等状态。
- 队列任务支持单个取消、失败重试、完成统计、成功率和总耗时展示。
- 历史记录新增关键词搜索、引擎筛选、收藏筛选和收藏星标。
- 参考图新增用途标注：主体参考、风格参考、构图参考。
- 参考图支持拖拽排序、清空全部、尺寸/体积展示与大图风险提示。

### 优化

- 上传参考图、蒙版重绘底图和生成请求内的 Data URL 会尽量自动压缩，降低请求体过大或接口拒绝的概率。
- 自动压缩参考图后同步调整蒙版尺寸，避免重绘区域与压缩后底图错位。
- 参考图、蒙版和用途标注现在会一起持久化到 IndexedDB / 本地文件夹。
- 历史记录保存逻辑统一到 `persistHistoryData()`，减少重复分支。
- 画幅选择器视觉样式收敛，降低高度、阴影和浮层感，让布局更稳定。
- 精简 `src/main.js` 入口导入，移除未使用的工具与存储依赖。
- 删除 `src/utils/helpers.js` 中未被引用的 URL 转 File 工具函数，减少遗留工具代码。
- 移除入口末尾重复执行的 `initMobile()`，避免移动端事件与状态重复初始化。

### 清理

- 将历史图片打包导出的目录名与压缩包文件名从旧品牌统一为 `DreamInk`。
- 更新指南弹窗内早期内测文案，减少旧版本品牌残留。

### 验证

- 已执行 `npm run build`，生产构建通过。
